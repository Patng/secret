$boxes = $('<%= j render(@communities) %>')

$('#communities').append( $boxes );
<% if @communities.next_page %>
  $('.pagination').replaceWith('<%= j will_paginate(@communities) %>');
<% else %>
  $('.pagination').remove();
<% end %>